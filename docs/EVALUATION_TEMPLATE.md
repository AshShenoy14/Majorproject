# Evaluation Report Template

Fill this in from `assets/evaluation/final_test_metrics.json` (generated by `python src/analysis/compare_models.py`); do not enter numbers that were not produced by the held-out test set.

**Model:** [Sequence (ESM-MLP) / Graph (GraphSAGE) / Random Forest / Ensemble (XGBoost)]
**Date:** [YYYY-MM-DD]
**Dataset split:** [Test set, N rows evaluated / N rows in file]
**Split design:** [e.g. stratified 80/10/10 pair-level, random_state=42; pair-disjoint, NOT node-disjoint (transductive)]
**Threshold:** [value selected on the validation set; state the selection rule]
**Checkpoint identifiers:** [SHA-256 prefixes from the JSON `checkpoints` block]

## 1. Quantitative Performance (test set only)

| Metric | Score |
| :--- | :--- |
| **Accuracy** | |
| **Precision** | |
| **Recall** | |
| **F1-Score** | |
| **ROC-AUC** | |
| **PR-AUC** | |

*Note: attach ROC and PR curves generated from the same test predictions.*

## 2. Model Comparison

| Model | Threshold | Accuracy | F1-Score | ROC-AUC | PR-AUC |
| :--- | :---: | :---: | :---: | :---: | :---: |
| ESM-2 + MLP | | | | | |
| GraphSAGE | | | | | |
| **Ensemble (XGBoost, proposed)** | | | | | |
| Random Forest (baseline) | | | | | |

State the number of runs/seeds. If only one run exists, say so and do not claim significance.

## 3. Explainability & Analysis

### Ensemble feature usage
- **Feature importance source:** [XGBoost gain or SHAP; say which]
- **Most-used feature(s):** [...]
- **Features never used by the trained trees:** [...]

### Network Analysis
- **Top central hubs:** [degree / betweenness / PageRank, and which graph they were computed on]

### Drug Targets
- **Candidates (ChEMBL-linked):** [...]
- Priority scores are heuristic and not validated; say so.

## 4. Scope and Limitations
- [Is the split node-disjoint? If not, state that the evaluation is transductive and does not measure unseen-protein generalization.]
- [Any dataset, negative-sampling or single-run caveats.]

## 5. Conclusion
[Summary of findings, strengths and limitations, restricted to what the evaluation supports.]
